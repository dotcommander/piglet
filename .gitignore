# Binary
/piglet
/lspq

# Scratch / exports
piglet-export-*.md
.work/

# Go workspace (local multi-module dev)
go.work
go.work.sum

# macOS
.DS_Store

# Secrets / config (safety net for forks)
*.env
auth.json

# Repomix output
repomix-output.md

# Stray pack build artifacts (go build ./extensions/packs/X/ without -o drops here).
# /core omitted because core/ is a tracked source directory.
/agent
/code
/context
/cron
/eval
/workflow
